export function greet(name: string): string {
    name = 'mosh'; //Se coloca el nombre en una ventana de texto o similar
    return `Hello, ${name}!`; // Devuelve un mensaje de saludo que incluye el nombre
}
